import { makeStyles } from '@material-ui/core';

const useModalStyles = makeStyles(() => ({
  input: {
    margin: '.5rem',
    width: '30rem',
  },
  ButtonsAction: {
    margin: '.5rem',
  },
}));

export { useModalStyles };
